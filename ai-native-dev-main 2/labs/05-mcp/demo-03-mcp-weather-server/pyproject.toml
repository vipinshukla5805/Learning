[project]
name = "demo-03-mcp-weather-server"
version = "0.1.0"
description = "MCP server integrating external Weather API to demonstrate real-world tool usage"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=0.1.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
]

[project.optional-dependencies]
testing = [
    "mcp>=1.0.0",  # Optional: for official MCP SDK client testing
]
