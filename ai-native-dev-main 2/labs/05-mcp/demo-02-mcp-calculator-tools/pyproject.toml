[project]
name = "demo-02-mcp-calculator-tools"
version = "0.1.0"
description = "MCP server with multiple calculator tools demonstrating parameter validation and error handling"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=0.1.0",
]

[project.optional-dependencies]
testing = [
    "mcp>=1.0.0",  # Optional: for test_client.py (official MCP SDK)
]
